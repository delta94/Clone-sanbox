__d("Pressability", ["WebPressability"], (function (a, b, c, d, e, f) {
    "use strict";
    a = importNamespace("WebPressability").usePressability;
    f.usePressability = a
}), 66);

import { usePressability as WebPressability } from 'WebPressability';

export const usePressability = WebPressability;