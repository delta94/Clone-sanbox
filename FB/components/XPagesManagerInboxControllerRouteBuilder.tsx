__d(
  "XPagesManagerInboxControllerRouteBuilder",
  ["jsRouteBuilder"],
  function (a, b, c, d, e, f, g) {
    a = c("jsRouteBuilder")(
      "/{page_token}/inbox/{?selected_item_id}/{?*extra_junk}/",
      Object.freeze({}),
      void 0
    );
    b = a;
    g["default"] = b;
  },
  98
);

import jsRouteBuilder from "jsRouteBuilder";

const XPagesManagerInboxControllerRouteBuilder = jsRouteBuilder(
  "/{page_token}/inbox/{?selected_item_id}/{?*extra_junk}/",
  Object.freeze({}),
  undefined
);

export default XPagesManagerInboxControllerRouteBuilder;
